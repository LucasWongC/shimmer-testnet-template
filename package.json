{
  "name": "shimmer-test-template",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ devlancer412/shimmer-test-template",
    "create-local": "graph create --node http://localhost:8020/ devlancer412/shimmer-test-template",
    "remove-local": "graph remove --node http://localhost:8020/ devlancer412/shimmer-test-template",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 devlancer412/shimmer-test-template",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.43.0",
    "@graphprotocol/graph-ts": "0.29.1"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
